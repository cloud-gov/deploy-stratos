applications:
  - name: ((name))
    routes:
      - route: ((route))
    memory: 512M
    disk_quota: 512M
    timeout: 180
    buildpacks:
    - binary_buildpack
    health-check-type: port
    instances: ((instances))
    services:
    - stratos-db
    command: ./jetstream
