applications:
- name: dashboard-beta-redirects
  buildpacks:
  - nginx_buildpack
  disk_quota: 1G
  instances: 1
  memory: 64M
  routes:
  - route: ((external-route))
  - route: ((app-route))
  stack: cflinuxfs3
  env:
    REDIRECT_URL: ((redirect-url))
